import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const SeriesPage = ({ series }) => (
    <View>
        <Text>Esta é a SeriesPages</Text>
    </View>
);

const styles = StyleSheet.create({

})

export default SeriesPage;